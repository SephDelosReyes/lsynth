return {
	sampleRate = 44100,
	bufferSize = 1024,
	frequency = 440,
	bufferQueueMax = 3,

	FILTER_CUTOFF = 1200,
	FILTER_CUTOFF_STEP = 150,
	FILTER_CUTOFF_MIN = 100,
	FILTER_CUTOFF_MAX = 8000,
	FILTER_CUTOFF_SCALE = 1.25, -- default for keyboard adjust
}
